from direct__nt as showbase
from avantpy.wrappers._translated import soft_remap, global_translate

SHOWBASE_MAP = {
  'ShowBase': 'TaispeánadhBonn'
}

# INITSCR_MAP = {
#   'clear': 'glanadh',
#   'getkey': 'faigheochair',
#   'addstr': 'cuirtea',
# }
# 
# import _curses
# soft_remap(_curses, 'initscr', INITSCR_MAP)

global_translate(globals(), direct, SHOWBASE_MAP)
